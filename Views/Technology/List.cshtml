@using Diamond.Models.Materials
@model IEnumerable<ProductionStage>
@{
    ViewData["Title"] = "Этапы производства";
}

<h1>Этапы производства</h1>
<input type="button" onclick="location.href='@Url.Action("Create", "Technology")'" value="Добавить этап производства" />
<table class="table table-bordered">
    <thead>
        <tr>
            <th>№</th>
            <th>Название</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.OrderBy(t=>t.Id)) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-Id="@item.Id" class="btn btn-primary">Редактировать</a>
                    <a asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger"
                       onclick="javascript:return confirm('Вы уверены, что хотите удалить данный элемент (@item.Name)?');">Удалить</a>
                </td>
            </tr>
    }
    </tbody>
</table>