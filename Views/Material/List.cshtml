@model IEnumerable<Diamond.Models.Material>
@{
    ViewData["Title"] = "Список сырья";
}

<input type="button" onclick="location.href='@Url.Action("Create", "Material")'" value="Добавить сырьё" />
<table class="table table-bordered">
    <thead>
        <tr>
            <th>№</th>
            <th>Название</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <a asp-action="Detail" asp-route-id="@item.Id">| Подробнее |</a>
                    <a asp-action="Delete" asp-route-id="@item.Id">| Удалить |</a>
                </td>
            </tr>
    }
    </tbody>
</table>